version: '3.5'

services:
  app:
    container_name: mllefantasia_pendureact
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 80
    env_file:
      - .env

networks:
    default:
       external:
         name: webproxy

